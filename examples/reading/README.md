# Reading wiki example

Chapter-by-chapter book notes with character and theme pages. A template for literary reading wikis.

```bash
wiki --root examples/reading search "gatsby"
wiki --root examples/reading list --type entity
```
# Research wiki example

A minimal research wiki with two ingested paper summaries. Use it as a template for academic or industry research notes.

```bash
wiki --root examples/research search "attention"
wiki --root examples/research ingest-status
```

Open `examples/research/` as an Obsidian vault alongside `examples/demo/`.